# Normalize line endings: store text as LF in the repo, check out native on Windows.
* text=auto

# Source and docs are text.
*.cpp   text
*.h     text
*.md    text
*.txt   text
*.qrc   text
*.rc.in text
*.cmake text
*.json  text
*.yml   text
*.yaml  text
*.manifest text

# Unix shell scripts and desktop entries must stay LF.
*.sh text eol=lf
*.desktop text eol=lf

# Windows shell scripts work best with CRLF on checkout.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets must never be normalized.
*.png binary
*.ico binary
*.zip binary
